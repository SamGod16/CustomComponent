# CustomComponent: InterruptorAnimado

Bienvenido al repositorio oficial del componente `InterruptorAnimado`. Este es un componente personalizado para JavaFX que implementa un botón de tipo *toggle switch* con animaciones fluidas y propiedades observables.

## 🚀 Instalación
Para utilizar este componente en tu proyecto Maven, primero debes clonar este proyecto y compilarlo localmente (o añadir el `.jar` al *classpath*).

1. Clona el repositorio:
   ```bash
   git clone [https://github.com/SamGod16/Componente-Future.git](https://github.com/SamGod16/Componente-Future.git)